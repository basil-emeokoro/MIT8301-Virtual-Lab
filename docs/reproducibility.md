# Reproducibility

Run `python scripts/run_pipeline.py`, `python scripts/build_submission.py`, `pytest -q`, and `python scripts/validate_submission.py` from the repository root. Random state 42 is fixed. `reports/tables/pipeline_results.json` records package versions and the UTC execution time.
