# Data

`raw/german_credit_data.csv` is the supplied targetless data. `raw/target_source/german_credit_with_risk.csv` is used only for exact target reconciliation. `processed/german_credit_with_verified_target.csv` is generated only after every predictor cell matches.
